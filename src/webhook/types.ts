export type MessageType = 'text'|'image'|'video'|'audio'|'document'|'sticker'|'unknown';
export interface MediaMetadata { mimetype?:string;size_bytes?:number;file_name?:string;width?:number;height?:number;duration_seconds?:number;caption?:string }
export interface WebhookPayload { version:'1.0';event:'whatsapp.message.received';event_id:string;session:string;occurred_at:string;delivery_context:'realtime'|'sync';account:{jid:string};data:{message_id:string;chat_id:string;sender_id:string;sender_pn_jid?:string;sender_phone?:string;sender_name?:string;from_me:boolean;is_group:boolean;message_type:MessageType;text?:string;timestamp:number;media?:MediaMetadata} }
